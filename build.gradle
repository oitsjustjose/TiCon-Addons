buildscript {
    repositories {
        jcenter()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:2.1-SNAPSHOT'
    }
}
apply plugin: 'net.minecraftforge.gradle.forge'

repositories {
  maven {
    url "http://dvs1.progwml6.com/files/maven"
  }
}

version = "1.0.1"
group= "com.oitsjustjose.tinkers_addons"
archivesBaseName = "Tinkers' Addons-1.9"

minecraft {
    version = "1.9-12.16.1.1923"

	if (file('../run').exists()){
		runDir = "../run"
	} else {
		runDir = "run"
	}    
    useDepAts = true
	replaceIn "lib/Lib.java"
    replace "@VERSION@", project.version
	
    mappings = "snapshot_20160312"
}

dependencies {
	deobfCompile "mezz.jei:jei_1.9:3.3.3.200"
    deobfCompile "slimeknights.mantle:Mantle:1.9-0.10.1.jenkins140"
    deobfCompile "slimeknights:TConstruct:1.9-2.3.1.jenkins226"
}

task deobfJar(type: Jar) {
    from sourceSets.main.output
    classifier = 'DEV'
}

artifacts{
	archives deobfJar
}

processResources
{
    // this will ensure that this task is redone when the versions change.
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    // replace stuff in mcmod.info, nothing else
    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
		expand 'version':project.version, 'mcversion':project.minecraft.version
    }
        
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}